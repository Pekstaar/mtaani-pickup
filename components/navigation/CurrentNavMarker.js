import {Box} from 'native-base';
import React from 'react';

const CurrentNavMarker = () => {
  return (
    <Box bg={'primary'} mt={'0.5'} h={'1'} w={'3'} borderRadius={'full'} />
  );
};

export default CurrentNavMarker;
