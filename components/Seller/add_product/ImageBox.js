import {Center} from 'native-base';
import React from 'react';

const ImageBox = () => {
  return (
    <Center
      borderRadius={'sm'}
      bg={'muted.300'}
      height={10}
      width={10}></Center>
  );
};

export default ImageBox;
